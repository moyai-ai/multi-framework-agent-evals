# Agent Instrumentor Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================================================
# REQUIRED: Claude Agent SDK
# ============================================================================
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# ============================================================================
# RECOMMENDED: Nia MCP Documentation Search
# ============================================================================
# Get your API key from: https://app.trynia.ai/
# Without this, the agent will use fallback instrumentation patterns
NIA_API_KEY=your-nia-api-key
NIA_API_URL=https://apigcp.trynia.ai/

# ============================================================================
# OPTIONAL: Self-Instrumentation with Langfuse
# ============================================================================
# These credentials enable observability of the agent-instrumentor itself
# Get your keys from: https://cloud.langfuse.com (or your self-hosted instance)
#
# This tracks the agent-instrumentor's own execution (tool calls, reasoning, etc.)
# This is SEPARATE from the instrumentation the tool adds to your target codebase
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# ============================================================================
# PLATFORM-SPECIFIC: Credentials for Target Codebase Instrumentation
# ============================================================================
# These are used when the agent-instrumentor generates instrumentation code
# for your target codebase. Include the credentials for your chosen platform:

# --- Langfuse (if instrumenting target codebase with Langfuse) ---
# TARGET_LANGFUSE_SECRET_KEY=sk-lf-...
# TARGET_LANGFUSE_PUBLIC_KEY=pk-lf-...
# TARGET_LANGFUSE_BASE_URL=https://cloud.langfuse.com

# --- LangSmith (if instrumenting target codebase with LangSmith) ---
# LANGCHAIN_API_KEY=ls-...
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_PROJECT=my-project

# --- Arize Phoenix (if instrumenting target codebase with Phoenix) ---
# PHOENIX_COLLECTOR_ENDPOINT=https://app.phoenix.arize.com

# --- DataDog (if instrumenting target codebase with DataDog) ---
# DD_SERVICE=my-service
# DD_ENV=production
# DD_VERSION=1.0.0

# ============================================================================
# NOTES
# ============================================================================
# 1. Self-instrumentation (Langfuse) is optional but highly recommended for
#    debugging the agent-instrumentor's execution and optimizing its performance
#
# 2. Target platform credentials (TARGET_*, LANGCHAIN_*, etc.) are optional
#    and only needed when you run the agent-instrumentor on a codebase
#
# 3. Never commit this file with real credentials! Keep it in .gitignore
